
VERSION="3.2"
DESC="Verify the Disk Group mapping to Virtual Instances per SDP"
ANS[0]="Good"
ANS[1]="Bad"
ANS[2]="N/A"
OSNAME="${OSNAME:-$( execl_zbksh 'uname -s' )}"
RESULT="${ANS[1]}"
REMARK="none"
RETCODE="99"
STATUS="${X}${X}${ANS[2]}"
STATUS="${X}${ANS[2]}"
QUOTED="${FALSE}"

ITEM_VGRP1="${FALSE}"
ITEM_VGRP2="${FALSE}"
ITEM_VGRP3="${TRUE}"

[[ "_${OSNAME}" == "_AIX"    ]] && OSCMD="/usr/es/sbin/cluster/utilities/cllsvg"
[[ "_${OSNAME}" == "_Linux"  ]] && OSCMD="[[ -x /usr/sbin/haconf ]] && /usr/sbin/haconf -makerw; [[ -x /usr/sbin/hastatus ]] && /usr/sbin/hastatus -sum"
[[ "_${OSNAME}" ==  _CYGWIN* ]] && OSCMD=""
