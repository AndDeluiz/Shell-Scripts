
VERSION="3.2"
HEADER="${TRUE}"
DESC="Physical Servers"
DESC="$( execl_zbksh 'if [[ -f /usr/sbin/virt-what ]] && /usr/sbin/virt-what | grep \"^\" > /dev/null 2>&1 ; then echo Virtual Servers ; else echo Physical Servers ; fi' )"
