
VERSION="3.2"
DESC="Verify Disk Size Requirements, volume group and file system layout as per SDP"
ANS[0]="Good"
ANS[1]="Bad"
ANS[2]="N/A"
OSNAME="${OSNAME:-$( execl_zbksh 'uname -s' )}"
RESULT="${ANS[1]}"
REMARK="none"
RETCODE="99"
STATUS="${X}${ANS[2]}"
QUOTED="${FALSE}"

ITEM_VGRP1="${TRUE}"
ITEM_VGRP2="${TRUE}"

[[ "_${OSNAME}" == "_AIX"    ]] && OSCMD="echo 'AIX Commands must be performed Manually'; ! : "
[[ "_${OSNAME}" == "_AIX"    ]] && OSCMD="echo 'VGs, LVs, and filesystems created according to SDP'"
[[ "_${OSNAME}" == "_Linux"  ]] && OSCMD="echo 'Linux Commands must be performed Manually'; ! : "
[[ "_${OSNAME}" ==  _CYGWIN* ]] && OSCMD=""
